rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads/writes for anyone signed in to debug the connection
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
